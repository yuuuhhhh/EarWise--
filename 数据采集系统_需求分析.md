# 二通道耳机脑电数据采集系统需求分析

文档日期：2026-09-25  
建设目录：`E:\疲劳检测\9月\mema`  
交付对象：负责实现系统的 Codex / 开发人员  
文档目的：据此开发可在本地运行的实验采集系统。本次交付仅为需求文档；不代表软件、虚拟环境或设备验证已经完成。

## 1. 建设目标与范围

系统使用现有 Python 蓝牙脚本中的通信与解码逻辑，采集耳机的两路真实脑电信号，完成被试登记、设备检查、30 秒基线、视频实验、主观量表及数据保存。

前端使用中文，简洁、清晰、美观。被试应能直接理解当前步骤；正式采集期间始终可见连接状态、左右通道饱和率及共享丢包率。

每次选择一个被试编号、一个天数编号和一个 round，执行该 round。**每次 round 尝试输出且仅输出三个正式数据文件：原始脑电、标签、配置快照。**不同 round 分目录保存；完整完成 12 个 round 时，有 12 组三文件，而不是全被试共用三份持续覆盖的文件。此项为本文采用的保存约定。

本期不要求账号体系、云端上传、脑电分类模型、复杂分析仪表盘或基于脑电推算专心分数。量表中的专心度、放松度和确信度均来自被试本人回答。

本文中“用户明确要求”是实验约束；“实现约定”是为开发提供的默认行为；“待确认/待验证”不得由开发人员默默猜测。参考脚本及其注释仅作为现有实现的证据，不视为新增需求。

## 2. 已有素材与现有脚本

已只读检查建设目录，发现：

```text
mema/
├─ bci-serve_get_data_origin_from_uuid_h21292_print_with_save.py
├─ attention_video/
│  └─ 01.mp4 … 12.mp4             # 当前共 12 个文件
└─ relax_video/
   └─ 01.mp4 … 04.mp4             # 当前共 4 个文件
```

不得删除、重命名或覆盖原始素材与参考脚本。新系统应拆分出设备通信模块，避免同时启动旧脚本和新系统抢占同一个蓝牙设备。

### 2.1 脚本中可确认的信息

| 项目 | 代码中的值 / 行为 |
|---|---|
| 目标设备名称 | `MindBridge-v3.11` |
| Service UUID | `0000ae30-0000-1000-8000-00805f9b34fb` |
| 写入特征 UUID | `0000ae01-0000-1000-8000-00805f9b34fb` |
| 通知特征 UUID | `0000ae02-0000-1000-8000-00805f9b34fb` |
| 帧格式 | 33 字节；首字节 `0xA0`；尾字节高四位 `0xC0` |
| 设备序号 | 零基偏移 1，即第 2 个字节；8 位无符号整数，范围 0–255 |
| 两路原始值 | 零基偏移 `[2:5]` 和 `[5:8]`，大端、有符号 24 位 ADC counts |
| 名义采样率 | `SAMPLE_RATE = 250` Hz；需要实机核对 |
| 现有换算 | `raw × (0.288486 / 12)`，脚本将结果称为 μV；物理单位与增益需要验证 |
| 实际发送的命令字节 | `S`、小写 `b`、`R`、`I` |

注意：原始模式按钮文字显示大写 B，但实际发送的是小写 `b`。不能只根据按钮名称更改命令大小写；原始模式、启动及停止行为须实机确认。脚本没有实现经验证的停止硬件数据流命令。

蓝牙通知可能包含半帧或多帧，必须保留流式缓冲与重组。当前解析仅检查帧头和帧尾，不能声称已实现 CRC 校验。

### 2.2 不可直接照搬的行为

- 脚本为了兼容其他软件模拟 8 通道，额外通道补零；正式系统只保存两路真实 EEG。
- 对外接口中的 `gains=96` 与换算用的 `ADS_GAIN=12` 不一致，不能将前者当成真实增益。
- 当前程序启动就创建记录文件；新系统只能在点击“开始 30 秒基线采集”且记录器准备成功后开始正式落盘。
- 当前默认额外保存 `.bin`；新系统默认不生成第四个正式数据文件。
- 当前尚无饱和率、实验流程、问卷、完整事件标签和可靠异常收尾。
- 当前部分“启动/停止”HTTP 接口仅返回文字，没有执行实际动作，不能当作可用设备控制接口。
- 当前写盘失败只打印错误；新系统必须立即在界面明确告警并终止当前正式流程。

## 3. 实验结构与视频分配

### 3.1 用户明确要求

| 项目 | 规则 |
|---|---|
| 被试编号 | 必填，由被试输入 |
| 天数编号 | 1、2、3 |
| round 编号 | 1、2、3、4 |
| 每个 round | 30 秒基线 + 1 个 attention trial + 1 个 relax trial |
| 每个 trial | 按用户最新确认，只播放 1 个视频，然后填写一次量表 |
| 单数 round | attention → 量表 → relax → 量表 |
| 双数 round | relax → 量表 → attention → 量表 |
| 总数 | 每名被试 12 个 round、24 个 trial，其中 attention 12 个、relax 12 个 |

round 奇偶按当前选择的 1–4 判断，不按跨天累计编号判断。每个 round 只有一次起始基线，不在两个 trial 之间增加基线。

### 3.2 最终视频数量与完整对应表

用户已明确更正：**每个 attention trial 播放 1 个视频，每个 relax trial 也播放 1 个视频。**因此每个 round 共播放 2 个视频，并填写 2 份量表。

attention 使用现有 12 个视频，按 day、round 顺序各播放一次。relax 使用现有 4 个视频；依据“重复使用”和 round 1–4 的设置，本文采用的分配约定为：**round 1/2/3/4 分别使用 relax 01/02/03/04，跨三个 day 重复**。该规则独立配置，便于实验负责人之后调整。

| day | round | attention 视频 | relax 视频 | trial 次序 |
|---|---|---|---|---|
| 1 | 1 | attention_video/01.mp4 | relax_video/01.mp4 | attention → relax |
| 1 | 2 | attention_video/02.mp4 | relax_video/02.mp4 | relax → attention |
| 1 | 3 | attention_video/03.mp4 | relax_video/03.mp4 | attention → relax |
| 1 | 4 | attention_video/04.mp4 | relax_video/04.mp4 | relax → attention |
| 2 | 1 | attention_video/05.mp4 | relax_video/01.mp4 | attention → relax |
| 2 | 2 | attention_video/06.mp4 | relax_video/02.mp4 | relax → attention |
| 2 | 3 | attention_video/07.mp4 | relax_video/03.mp4 | attention → relax |
| 2 | 4 | attention_video/08.mp4 | relax_video/04.mp4 | relax → attention |
| 3 | 1 | attention_video/09.mp4 | relax_video/01.mp4 | attention → relax |
| 3 | 2 | attention_video/10.mp4 | relax_video/02.mp4 | relax → attention |
| 3 | 3 | attention_video/11.mp4 | relax_video/03.mp4 | attention → relax |
| 3 | 4 | attention_video/12.mp4 | relax_video/04.mp4 | relax → attention |

计算规则：`attention_index = (day - 1) × 4 + round`；`relax_index = round`。编号按两位补零。素材数量与此方案匹配；当前仅检查了文件存在，播放兼容性与时长应在开发时验证。

### 3.3 素材配置要求

- 视频对应关系放在独立的项目级 `config/video_manifest.json`，覆盖全部 12 个 day×round 组合。正式实验不随机排序。
- 每项包含 day、round、condition 和对应视频；每个视频有稳定 ID、相对路径、文件名。不同目录内的 `01.mp4` 应使用不同 ID，例如 `attention_01`、`relax_01`。
- 若内部使用视频列表结构，每个 trial 的列表长度必须严格为 1；不能沿用最初描述中的“两视频/trial”。
- 预检当前 round 的文件存在性、可读取性、浏览器可播放性及播放时长；正式启用前还须检查完整 12 轮映射。缺素材时给出具体文件路径并禁止进入相关 round。
- 时长读取自媒体元数据；完整播放视频，不硬编码时长、不随机截断、不默认循环、不变速。保留原视频音轨，不擅自静音；声音设置在设备检查时确认并记录。
- 配置缺失或不符合上述分配规则时，显示具体校验错误；不能使用临时示例配置冒充正式方案。
- 每次正式开始时把实际选用的视频清单、时长及文件 SHA-256 摘要写入本次配置快照，后续修改项目清单不得改变既有记录。

## 4. 被试操作流程和页面

### 4.1 登记与设备检查

1. 输入被试编号，选择 day 1–3、round 1–4；下拉框初始显示“请选择”，避免误用默认值。
2. 后端校验输入。被试编号作为字符串保存，保留 `001` 等前导零，去除首尾空白，拒绝空值、路径分隔符和路径穿越字符；生成的文件路径必须始终位于数据目录内。
3. 显示本轮 trial 顺序及视频清单摘要，不要求被试填写视频路径。
4. 连接耳机并启动经验证的原始数据预览流，显示设备状态和实时质量信息。
5. 只有素材配置有效、左右映射已确认、耳机已连接、近期收到有效 EEG、输出目录可写且记录器可用时，才能点击“开始 30 秒基线采集”。

仅连接成功但没有有效数据时，显示“耳机已连接，等待脑电数据”，不得显示“可以采集”。检查阶段可以接收 EEG 用于质量显示，但不将其混入正式数据文件。

不得擅自要求必须按 day 或 round 顺序参加实验；支持选定合法编号补采。若同一被试/day/round 已有完成记录，提示已有记录时间；确认重采后创建新尝试，不能覆盖。

### 4.2 30 秒基线

- 点击开始后，由后端一次性完成会话建立、三文件初始化和正式采集开关，成功后记录 `SESSION_START`、`BASELINE_START` 并启动倒计时。
- 正式开始时重置本轮保存行号、统计计数和质量窗口；不把预览阶段的样本、丢包计数或饱和计数并入本轮结果。质量窗口重建期间如实显示“数据不足”。
- 基线页面显示“基线采集中”、30 秒倒计时及简短引导。睁眼/闭眼、注视点等条件尚未指定，开发人员不得自行写入实验指令；提供可配置的引导文案并在使用前由实验负责人填写。
- 以服务端单调时钟计满 30 秒。不能因丢包而等待凑够 7500 行并悄悄延长基线。
- 正常结束记录 `BASELINE_END`，进入第一个 trial 的视频准备阶段。基线至视频真正播放之间标为 `transition`。

### 4.3 视频 trial

- 每个 trial 只播放配置对应的一个视频，完整播放结束后进入该 trial 的量表；两个 trial 之间以第一份量表衔接。
- 视频按原始宽高比展示，主体区域尽可能大；隐藏拖动进度、倍速、跳过和循环等被试控制项。
- 基线结束后自动尝试播放；若浏览器阻止自动播放，显示“点击开始播放”，等待期间标为 `transition`，不能提前记为已播放。
- 预加载本 round 视频。记录实际 `playing`、`waiting`、`pause`、`ended`、`error` 事件和媒体播放位置；禁止用点击时刻或视频标称时长替代实际播放事件。
- 该 trial 的视频首次实际开始时记录 `TRIAL_START`；该视频正常结束时记录 `TRIAL_END`。缓冲/卡顿仍须单独标注，不能只凭整个 trial 区间推断持续观看时间。
- 短暂缓冲单独标注为 `video_buffering`，恢复播放时记录对应事件；不把缓冲当成正常观看。持续播放失败按异常中断处理，不自动跳过视频。
- 不额外添加固定休息时长、视频间黑屏时长或新的实验刺激；如后续需要，另行更新配置与文档。

### 4.4 每个 trial 后的量表

| trial | 第 1 题 | 第 2 题 |
|---|---|---|
| attention | 你刚刚的专心程度是多少？ | 你对刚刚专心程度评分的确信度是多少？ |
| relax | 你刚刚的放松程度是多少？ | 你对刚刚放松程度评分的确信度是多少？ |

- 两题都只能选择整数 1、2、3、4、5；无默认选中项，两题完成才能提交。
- 建议程度两端文案为“1＝非常不专心/非常不放松，5＝非常专心/非常放松”；确信度为“1＝非常不确信，5＝非常确信”。最终措辞在配置中留档。
- 每份量表评价刚结束的 trial 中该段视频的体验，关联唯一 `trial_id`，不同时评价另一个条件的视频。
- attention 只填写 `attention_score` 和 `confidence_score`；relax 只填写 `relax_score` 和 `confidence_score`。不适用的评分留空，不写 0。
- 量表阶段继续保存原始脑电，阶段标为 `questionnaire`；评分通过 trial_id 关联到前一个 trial，不把量表期间 EEG 标成 attention/relax。
- 提交须由后端校验并幂等保存；快速双击、请求重试不能重复生成评分或跨过下一 trial。
- 第一份量表提交后进入另一条件 trial。第二份量表提交并持久化后结束本轮采集，执行保存校验。

### 4.5 完成页与视觉要求

完成页显示被试编号、day、round、完成状态和本轮保存目录，列出三个文件。只有文件写入、关闭和配置收尾成功后才能显示“本轮采集完成，数据已保存”。

推荐页面布局：顶部为被试/day/round 和当前步骤；中央为登记表、基线倒计时、视频或量表；右侧或底部为固定设备状态区。白色或浅灰底、少量主色、大号清晰文字，状态使用文字和图标配合颜色，不仅依赖红绿色区分。

视频和问卷时避免无关图表、弹窗或装饰动画。默认不显示实时脑电曲线或算法专心评分。任何全屏布局都必须保留设备状态区。

## 5. 设备状态、饱和率和丢包率

### 5.1 固定状态区

至少显示：

- 蓝牙状态：搜索中 / 连接中 / 已连接 / 已断开 / 重连中。
- 数据状态：等待数据 / 正常接收 / 数据中断；最近有效 EEG 的接收时间或距今时长。
- 左通道饱和率、右通道饱和率。
- 共享丢包率，明确标注“左右通道共用”。若放在两个通道卡片内，均须说明是同一链路指标。
- 正式状态：预览中 / 正式采集中 / 保存中 / 已完成 / 已中断。

默认每秒刷新一次，指标使用最近 5 秒滑动窗口。无数据、数据不足或过期时显示“暂无数据/数据不足/数据已中断”，不得显示假 0%。窗口尚未积满时可以显示当前估计，但必须同时标明实际窗口长度。

左右通道映射需经接线资料或实机核对。未确认前只能显示“通道 0”“通道 1（左右待确认）”，不能擅自声称通道 0 一定对应左耳。

### 5.2 饱和率定义

两通道分别使用各自未经滤波的原始 ADC counts，统计滑动窗口内达到削顶阈值的有效独立样本比例：

```text
某通道饱和率 = 该通道达到饱和阈值的样本数 / 该通道有效独立样本数 × 100%
```

阈值依据实际 ADC/设备削顶范围确定，并写入配置。用于联调的默认估计可以采用 24 位有符号数字范围的 98%：`raw <= ceil(-8388608 × 0.98)` 或 `raw >= floor(8388607 × 0.98)`；此时界面应注明“按配置阈值估计”。阈值比例 0.98 是工程初值，不是已经验证的设备参数或实验剔除标准。

真实阈值、特殊状态值及算法须在正式使用前验证。重复数据不重复计入分母；分母为 0 时值为空。全零、平直或噪声大不等于饱和，不得用这些条件替代削顶定义。

### 5.3 丢包率定义与限制

一个有效 33 字节帧同时包含两通道，使用相同设备序号，因此只能得到共享帧丢失指标，不能得到互相独立的左右丢包率。

在可信连续流区段内，根据设备 8 位序号统计：

```text
共享丢包率估计 = 推定丢失帧数 / (收到的唯一有效帧数 + 推定丢失帧数) × 100%
```

- 统计单位是解出的 EEG 数据帧，不是 BLE 通知次数。
- 正常序号 `254 → 255 → 0 → 1` 不丢包；短时可信区段的 `10 → 13` 推定缺失 11、12 两帧。
- 重复帧单独计数，不能增加唯一接收数而稀释丢包率；乱序、设备复位不能直接当成大批丢包。
- 8 位序号在名义 250 Hz 下约 1.024 秒回绕，序号本身不能唯一识别整圈缺失。即使序号差为 1，也可能存在整圈丢失。界面与文档应将其称为“基于序号的丢包率估计”。
- 出现断连、长时间无有效数据、复位或歧义序号时，记录未知缺失区间，新建 `stream_epoch_id`，清空帧重组缓存和序号基准。不得把断开前后序号相减并称为精确缺失数。
- 连续性可信条件、滑动窗口记账方式和算法版本必须配置并留档。默认无有效帧超过 1 秒即触发数据中断；该阈值须结合实际 BLE 批量到达情况验证。
- 不可判定的区间显示“无法准确估计/数据中断”，不强行给出 0% 或 100%。基于时间推算的缺失量如有实现，必须另列为估计，不能混进序号统计。

每秒把质量快照记入同一个标签文件，包含窗口范围、分子/分母、左右饱和率、共享丢包率、重复帧数、未知缺口状态及算法版本，便于事后核对。质量提示默认不自动剔除数据，也不凭未确认阈值自行改变实验次序。

## 6. 连续采集、状态和异常处理

### 6.1 正常状态流

```text
登记 → 设备检查 → 基线(30秒)
                  ↓
             trial 1 视频 → 量表 1
                  ↓
             trial 2 视频 → 量表 2
                  ↓
               保存校验 → 完成
```

后端是唯一流程状态来源，前端不能通过手动请求跳过基线、视频或问卷。前端刷新后读取已有会话及其中断状态，不新建另一个采集任务；正式阶段的刷新按下方中断规则处理。同一后端同时只允许一个正式会话；其他页面只能查看状态，不能重复控制。

从 `BASELINE_START` 至第二份量表成功提交之间连续采集，包括视频切换、短暂缓冲和量表。除真实没有接收数据外，不人为截断原始流。阶段细分为 `baseline / attention / relax / transition / questionnaire / video_buffering`；trial 条件和当下实际阶段应分开记录。

### 6.2 默认中断策略

以下为保护记录可解释性的实现约定，不是后续分析的自动剔除标准：

- **蓝牙断开或数据流中断：**立即暂停视频、停止正常实验推进，提示原因，尽力写入中断事件并结束当前会话，状态为 `interrupted`。后台可重连回到预览，但不能自动续播或把新流并入已结束会话。
- **再次尝试：**重新执行该 round 时建立新的 session 和 30 秒基线，保留前次文件及重复尝试关系；是否安排重采由实验操作人员决定。
- **被试主动中止：**提供明确的“中止本轮”入口，确认后保存已采集内容，状态为 `aborted`。
- **视频异常：**短暂卡顿如实标注；持续卡顿或错误不能跳过视频假装完成。建议持续不可播放 5 秒进入中断，阈值可配置。v1 不提供自由暂停后仍当作无中断完成的功能。
- **刷新/关闭页面：**如果发生在基线、视频或量表阶段，按中断处理，不在后台继续悄悄播放或计时。使用页面心跳检测意外关闭；默认 3 秒未收到心跳时中断，超时须可配置。心跳绑定本次会话的控制页面，其他只读页面的心跳不能维持该会话。设备检查页刷新不产生正式会话。
- **写盘失败/磁盘满/写入队列持续积压：**立即停止正常实验推进并告警，状态为 `save_failed`；尽力保存已有文件和错误信息，不承诺失败时还能完整写入三文件。
- **进程意外退出：**已成功写出的原始数据和标签保留；下次启动把上次进程遗留的 `recording` 会话标为 `interrupted`。既有 `aborted / interrupted / save_failed` 状态和失败原因保留，不得覆盖或显示已完成。

不能补零、复制上一样本或插值后冒充原始 EEG。v1 不实现跨中断无缝续采；如后续需要，应另行定义恢复规则、标签及实验有效性判定。

### 6.3 保存可靠性

采集、视频控制和磁盘写入解耦；使用有界队列和独立写入任务，监控队列积压，不能为保持页面流畅静默丢弃原始数据。

原始数据和事件边采边写，默认至少每秒刷新；阶段结束、问卷提交、正常完成和中止时立即刷新，必要时同步到磁盘。配置快照使用临时文件安全替换；临时文件不属于最终交付文件。正常完成时检查数据/事件/问卷完整性后再标为 `completed`。突然断电仍可能丢失尚未持久化的尾部，不能承诺绝对零丢失。

## 7. 时间基准和标签对齐

1. EEG 接收、基线计时、服务端事件统一使用同一进程的单调时钟；同时记录 UTC 墙钟时间，便于关联外部记录。
2. 原始数据中的接收时间代表主机收到数据的时刻，不代表设备物理采样时刻。多帧由同一次 BLE 通知带来时，可共享接收时间，并记录通知编号与帧内顺序。
3. 连续 `sample_row_index` 只是保存行号，不是设备序号，也不是保证无缺失的生理样本编号。
4. 若另加按 250 Hz 推算的样本时间，字段必须命名为估计值，并保存分段锚点与假设；不得跨数据缺口强行填满等间隔时间轴。第一版可以只保存真实接收时间及序号。
5. 浏览器的媒体事件通过本地实时连接发送；记录前端事件发生时刻、媒体位置、服务端接收时刻。使用往返校时把前端时间映射到后端单调时钟，记录同步误差估计，不能直接相减两个不同来源的时钟值。
6. 前端播放事件映射时间用于估计视频边界，服务端接收时间用于审计；保存最近已接收的 EEG 行号以辅助对齐，不能声称该行就是刺激开始时的精确采样点。
7. 基线/视频/量表区间采用“包含开始、不包含结束”的规则。评分作为独立结果记录，不是与 baseline/attention/relax 并列的互斥阶段标签。
8. 本系统是软件级时间标记。未增加光电触发器等验证装置时，不宣称具有屏幕真实呈现时刻的毫秒级同步精度。

## 8. 三个正式输出文件

### 8.1 目录与命名

```text
data/
└─ sub_001/
   └─ day_01/
      └─ round_01/
         └─ <session_id>/
            ├─ eeg_raw.csv
            ├─ labels.csv
            └─ config.json
```

`session_id` 使用时间加随机标识或 UUID，确保快速重试也不会碰撞。三文件均包含/关联同一 session_id。项目配置、程序日志和虚拟环境不属于实验结果三文件，放在会话目录以外。默认不额外生成 `.bin`、评分 JSON、第四份质量 CSV 或数据库作为必需的数据交付物。

CSV 使用 UTF-8、标准 CSV 引号规则和固定表头；小数点为 `.`，空值为空字段，不使用 0 代替未知。JSON 使用 UTF-8。以下字段为开发契约，可增加字段，不得缺少其表达的信息；`config.json` 中的 `schema_version` 同时定义三文件的格式版本。

### 8.2 原始脑电：`eeg_raw.csv`

每行对应一个实际接收并成功解析的两通道 EEG 帧，不保存由算法补出的伪样本。

| 字段 | 含义 |
|---|---|
| `session_id` | 本次 round 尝试唯一标识 |
| `sample_row_index` | 从 0 开始连续递增的保存行号 |
| `stream_epoch_id` | 连续流区段标识 |
| `notification_id`、`frame_in_notification` | BLE 通知编号和解出帧的顺序；跨通知拼帧按完成重组的通知归属 |
| `device_seq` | 原始设备序号 0–255 |
| `received_at_utc` | 主机接收时间，ISO 8601，包含时区 |
| `received_monotonic_ns`、`elapsed_ms` | 后端单调时间及相对正式起点时间 |
| `channel_0_raw`、`channel_1_raw` | 两路未经滤波的有符号 24 位 ADC counts，整数 |
| `channel_0_uv`、`channel_1_uv` | 可选换算值；单位/比例未经验证时留空 |
| `frame_status` | `normal / duplicate / sequence_ambiguous` 等解析后状态 |

为保持接收事实，重复/序号异常的可解码帧仍可保存并标记，计算质量时按定义排除重复；格式错误、无法解码的字节不伪造成 EEG 行，通过标签记录解析错误计数。左右映射存入 config，不因 UI 展示顺序而丢失物理通道编号。

保存原始 counts，不用滤波、去噪、平滑、重采样或归一化后的结果覆盖它们。可选 `original_frame_hex` 字段用于追溯原始帧，但不得为此默认新增第四个文件。

### 8.3 标签与量表：`labels.csv`

采用统一、可追加的事件表；通过开始/结束事件重建阶段区间，通过 `trial_id` 关联评分，避免另建量表文件。

| 字段 | 含义 |
|---|---|
| `session_id`、`event_id`、`event_type` | 会话、唯一事件 ID、事件类型 |
| `event_time_utc`、`event_monotonic_ns`、`elapsed_ms` | 事件统一时间；前端事件为校时映射值 |
| `server_received_monotonic_ns` | 后端收到事件时间 |
| `client_event_id`、`client_performance_ms`、`sync_uncertainty_ms` | 前端去重/时间审计字段；不适用时为空 |
| `last_sample_row_index`、`stream_epoch_id` | 事件处理时最近接收的 EEG 行号及流区段；尚无样本时为空 |
| `stage` | baseline、attention、relax、transition、questionnaire、video_buffering 等 |
| `trial_id`、`trial_order`、`condition` | trial 唯一标识、当前 round 内第 1/2 个、attention/relax；基线为空 |
| `video_id`、`video_order_in_trial`、`media_position_ms` | 对应视频及播放位置；不适用时为空 |
| `attention_score`、`relax_score`、`confidence_score` | 仅评分事件填写相应整数 1–5，其余留空 |
| `details_json` | 原因、质量快照、窗口计数、错误详情等结构化扩展；正确进行 CSV 转义 |

至少覆盖下列事件：

```text
SESSION_START / SESSION_END
BASELINE_START / BASELINE_END
STAGE_START / STAGE_END
TRIAL_START / TRIAL_END
VIDEO_PLAYING / VIDEO_ENDED / VIDEO_WAITING / VIDEO_PAUSED / VIDEO_ERROR
QUESTIONNAIRE_START / RATING_SUBMITTED
BLE_DISCONNECTED / BLE_RECONNECTED / DATA_TIMEOUT / DATA_GAP / SEQUENCE_ANOMALY
QUALITY_SNAPSHOT
SESSION_ABORTED / SESSION_INTERRUPTED / SAVE_ERROR
```

`VIDEO_PLAYING` 可在缓冲恢复时再次出现；通过事件顺序区分首次开始和恢复，不重复创建 trial。`STAGE_START/END` 记录实际阶段切换，基线专用事件与阶段事件可共享同一边界时间。会话结束时关闭尚未结束的阶段并记录结束原因。

原始数据行按接收顺序追加；事件按接收顺序追加但包含实际/映射发生时间，分析不得误以为文件顺序必然等于前端事件时间顺序。前端事件 ID 与状态变更必须幂等处理。

### 8.4 本轮配置快照：`config.json`

必须保存实际生效值，不能只放一个指向日后可能变更的项目配置文件路径。至少包含：

- `schema_version`、`session_id`、被试编号、day、round、尝试编号、关联前次尝试的 `retry_of_session_id`（适用时）。
- 软件版本/代码版本、运行环境版本、模拟或真实设备模式。
- `session_status`：`recording / completed / aborted / interrupted / save_failed`，起止 UTC 时间、单调时钟起点、终止原因、当前/最后阶段。
- 基线时长 30 秒、基线引导文案、trial 实际顺序、全部实际选用视频的 ID/路径/摘要/时长、量表题目及 1–5 选项文案。
- BLE 名称、UUID、已核对的命令字节、协议参数、名义采样率及其验证状态。
- 两通道原始编号到左右耳的映射及验证状态；ADC 位数、增益、候选/已确认换算系数、单位及验证状态。未经验证时保留 counts，不输出伪精确 μV。
- 饱和率窗口/阈值/算法版本、丢包率窗口/算法版本、数据中断阈值、页面心跳阈值、播放错误阈值、刷新频率和中断策略。
- 时间同步方式、时钟锚点、误差估计方法，明确无设备采样时钟时的限制。
- 结束摘要：实际行数、唯一有效帧数、重复帧数、可估计丢失帧数、未知缺口、两份量表是否提交、各 trial 完成情况、数据/事件文件名。

配置中的实验参数在会话启动后冻结，仅允许补充运行结果和结束摘要。评分的唯一权威来源是 labels；配置可记录提交状态，不产生互相冲突的另一套评分。

## 9. 开发环境与实现边界

### 9.1 必须先建立项目虚拟环境

开发工作在 `E:\疲劳检测\9月\mema` 内进行。第一项实现操作是建立本地 `.venv`，后续依赖安装和启动均使用它；不得把项目依赖装入全局 Python。

PowerShell 示例：

```powershell
Set-Location -LiteralPath 'E:\疲劳检测\9月\mema'
py -3 -m venv .venv
# 开发人员创建并验证 requirements.txt 后执行：
& .\.venv\Scripts\python.exe -m pip install -r requirements.txt
# 约定实现 app/main.py 作为启动入口后执行：
& .\.venv\Scripts\python.exe -m app.main
```

无需强制修改 PowerShell 全局执行策略，直接调用虚拟环境内的 Python 即可。若 `.venv` 已存在，先确认其可用性，不删除用户已有环境。开发交付中提供使用虚拟环境的一键启动脚本及中文 README，并记录已实际验证的 Python 和依赖版本。

### 9.2 建议实现方式

- Windows 本地 Python 后端负责 BLE、帧解析、质量计算、实验状态、计时、事件和落盘；浏览器负责页面、视频及问卷。
- 可沿用现有 `bleak`、`asyncio` 和 Tornado，配合 HTML/CSS/JavaScript 及 WebSocket，减少无关依赖。框架可调整，但应保持职责清晰。
- 浏览器不直接连接蓝牙。前端质量显示失败不能影响原始接收与落盘；连接控制和状态转换以服务端为准。
- 服务只绑定 `127.0.0.1`，端口可配置，例如 8765；不沿用占用管理员端口 80 的默认行为。无需云端服务。
- 原始设备通信、解码、质量指标、实验状态和文件写入拆为独立模块，便于用模拟数据验证。
- 保留原有 UDP 转发仅作为兼容选项，默认关闭；正式采集不依赖补零成 8 通道后的转发结果。
- 路径以项目目录定位，兼容中文目录及非项目目录启动。视频路径限制在指定素材目录，避免把用户输入直接拼接成任意文件路径。

建议新增目录：

```text
mema/
├─ .venv/
├─ app/                     # 后端、设备、状态机、写入器
├─ web/                     # 页面、样式、前端逻辑
├─ config/                  # 项目参数与视频清单
├─ data/                    # 每轮三文件结果
├─ tests/                   # 对数据正确性有意义的测试
├─ requirements.txt
├─ start.ps1
└─ README.md
```

原有两个视频目录及参考脚本继续保留。不要把视频复制进程序包，也不要自动生成假视频填补正式素材。

### 9.3 接口能力

接口名称可由实现确定，但至少提供：输入和视频计划预检、设备连接/预览状态、实时质量订阅、创建并开始 round、接收媒体事件、提交指定 trial 量表、中止会话、读取会话状态和保存结果。

服务端对每个状态变更进行参数及阶段校验；重复请求返回既有结果，不重复开启记录器，不重复发送启动命令。浏览器断开与设备断开分别处理，不能混成一个状态。

## 10. 验收标准

| 编号 | 场景 | 必须满足的结果 |
|---|---|---|
| A01 | 项目启动 | 使用建设目录内 `.venv` 启动；现有脚本/视频未被覆盖；普通本地账号可打开页面 |
| A02 | 输入校验 | 空编号、非法 day/round 被阻止；`001` 保留前导零；路径穿越输入不能改变输出目录 |
| A03 | 视频映射 | 12 个组合与第 3.2 节完全一致；attention 01–12 各一次，relax 按 round 跨天重复，每个 trial 一个视频；缺素材在正式开始前报出具体路径 |
| A04 | 顺序 | round 1/3 为 attention→relax，2/4 为 relax→attention；跨天规则不变 |
| A05 | 基线 | 每轮仅一次 30 秒基线；接收样本数量不足不延长计时；预览数据不写入正式文件 |
| A06 | 视频与量表 | 一个 trial 完整播放一个视频后仅出现一份对应量表；一轮恰好两个视频、两份量表 |
| A07 | 评分 | 两题均为必填 1–5 整数，无默认值；评分属于正确 trial；双击不重复提交 |
| A08 | 原始解码 | 正数、负数、24 位边界值、拆包/粘包均正确；只保存两路真实原始值；不补零或滤波覆盖原值 |
| A09 | 丢包估计 | 覆盖正常回绕、已知短缺口、重复、复位、乱序和长时中断；歧义区段不输出伪精确结果 |
| A10 | 饱和率 | 注入已知比例的阈值样本，两通道计算独立且正确；无样本不显示 0%；阈值和映射可追溯 |
| A11 | 连接状态 | 区分已连接无数据、正常接收和断连；当前质量值不会在断连后继续冒充实时值 |
| A12 | 时间标签 | 基线、实际视频播放、缓冲、过渡、问卷及评分均可重建；保留前端发生和后端接收时间 |
| A13 | 正常保存 | 每个 session 最终恰好三个结果文件，身份一致，两个 trial 和两份评分齐全；保存成功后才显示完成 |
| A14 | 中断和重采 | 断连/无数据时视频暂停且会话中断；重连不自动续播；重采使用新 session 并保留前次数据 |
| A15 | 失败与退出 | 磁盘失败、页面关闭、进程退出可识别为未完成；已有可读数据保留；不能错误标记成功 |
| A16 | 重复控制 | 双击开始、多开标签页、网络重试均不会出现两个并行记录器或跳过状态 |

先用模拟数据覆盖关键数据正确性与异常路径，再用真实耳机完成至少一轮奇数 round 和一轮偶数 round。模拟模式须明显标识并写入配置，不能混入正式结果目录。只能声称已经实际完成的验证；没有耳机时应交付可运行软件并列明待实机验收项，不能宣称硬件通过。

## 11. 实施顺序和交付要求

1. 建立 `.venv`，整理依赖；保留素材和参考脚本。
2. 实现设备通信与双通道解码，核对原始模式命令、实际采样率、左右映射和换算/饱和参数。
3. 按第 3.2 节实现完整 12 轮视频清单与配置校验。
4. 实现后端状态机、三文件记录器、时间标记和异常收尾，再接入页面操作。
5. 完成中文前端、媒体事件和问卷；实时显示质量指标。
6. 完成模拟与实机验收、中文 README、一键启动脚本，附一组明确标为模拟的三文件示例。

开发交付须包含可运行源代码、环境依赖、配置说明、启动方法、数据字段字典、测试结果及待验证事项。不可用的功能不能用静态数值或假数据冒充成功。

## 12. 正式实验启用前核对清单

- [x] 每个 attention trial 使用一个视频，12 个素材按 day、round 顺序使用。
- [x] 每个 relax trial 使用一个视频；本文采用 round 对应 01–04、跨天复用的分配约定。
- [ ] 项目视频清单覆盖 12 个 day×round 组合，所需素材齐备且可播放。
- [ ] 左右耳映射、原始模式命令、250 Hz 名义采样率得到实机核对。
- [ ] ADC 削顶定义得到核对；换算未经验证时保存 counts 并将 μV 留空。
- [ ] 基线引导和声音设置由实验负责人明确，并写入配置快照。
- [ ] 至少完成一次奇数 round 和一次偶数 round 的真实全流程及三文件检查。

开发时可并行推进不依赖硬件验证的部分，但不得默默改变视频分配、trial 数量或量表流程。
