# EarWise采集系统 · 软件测试报告

## 当前版本 v1.3.0：统一使用 Round，保存身份逐行校验

当前协议为 `earwise-3round-4trial-randomstart-v4`，共 3 个 round，每轮 4 个 trial（2 个 attention、2 个 relax），随机决定首条件后严格交替。Round 1/2/3 分别使用 attention_01–02、03–04、05–06，每轮复用 relax_01–02。每轮一次静息 30 秒、四份量表；可按每天一轮安排，系统不以日历天数编号。

界面、API、素材清单、随机分配和计划指纹都使用实际 `round=1–3`。新数据 schema 为 `2.0`，`config.json` 不含 `day`，两份 CSV 的前三列均为 `session_id,subject_id,round`；路径为 `data/sub_<被试>/round_0N/<session>/`（模拟数据根目录为 `simulation_data`）。收尾时逐行比对 EEG、标签与配置身份，错误编号不能作为保存成功。已有旧协议记录保留，不计入当前协议尝试；未完成旧记录恢复时沿用原表头，避免列错位。

完整回归 **130 项全部通过，20.753 秒**：配置 26 项、协议质量 18 项、控制器 30 项、记录器 28 项、HTTP 11 项、启动实例复用 17 项。`node --check web/app.js`、`setup.ps1 -CheckOnly`（8/8 视频）及 `git diff --check` 均通过。

控制器测试包含 **3 轮 × 2 种首条件，共 6 种完整流程**，真正写入再读取 CSV/JSON，检查四个 trial、四份量表、事件与视频关联、30 秒逻辑基线、目录及每行被试与 Round 编号。还覆盖缺失或非法 round、旧 day 请求、过期/其他轮次计划、身份篡改、错轮视频、重采与历史记录隔离、旧表头恢复。控制器的逻辑时钟测试不代替浏览器真实计时与播放验收。

2026-09-25 在隔离目录 `work/round-label-browser-fixture/` 使用实际浏览器、模拟 EEG 和八段 4 秒测试视频，完成以下两轮。14 个 app/web/config 文件与产品源码 SHA256 一致，基线真实等待 30 秒，视频原速完整播放，无跳转、倍速或假时钟；正式实验素材未替换。

| 测试被试 | Round | 实际视频顺序 | 四份程度评分（确信度均为4） | EEG行数 | 标签行数 |
|---|---|---|---|---:|---:|
| 001 | 2 | A03 → R01 → A04 → R02 | 2、5、1、4 | 26,630 | 181 |
| 001 | 3 | R01 → A05 → R02 → A06 | 5、2、4、1 | 36,450 | 220 |

两次浏览器完成页均显示实际 Round、四个 trial、四份量表与三文件保存校验成功，目录分别为 `sub_001/round_02/<session>/`、`sub_001/round_03/<session>/`。每份量表初始为空，前两份提交后继续后两个 trial。浏览器错误日志为空。独立运行 `work/verify_round_labels_browser.py` 复读保存结果，确认每个 EEG 和标签行都保留被试 `001`，轮次分别为 `2`、`3`，JSON/CSV 无 `day`，身份、随机分配、计划指纹、视频条件顺序、四份评分、事件计数及恰好 30 秒基线全部通过；报告在 fixture 的 `work/browser-result-verification.json`。

原项目已通过模拟启动脚本重启为 v1.3.0，实际页面仅有 Round 1–3 选择，8 个原视频均通过时长与首帧检查，并核对 Round 3 的 R01 → A05 → R02 → A06 计划。短视频全流程不代表逐秒播放全部原视频；测试评分仅用于软件验收，真实耳机连接、左右耳归属、实际声音输出和物理时间同步仍需实机验证。

## v1.2.0 历史测试记录：每天一轮、随机起始交替

以下记录属于已由 Round 编号替代的旧版 Day 协议，不代表当前数据格式。

当前协议为 `earwise-3day-1round-4trial-randomstart-v3`，共 3 天，每天 1 轮、4 个 trial（专心和放松各两个），每轮一次静息 30 秒和四份量表。被试编号与天数决定可复现伪随机首条件，随后严格交替；预览、刷新、服务重启和同日重采使用一致顺序。界面不再选择轮次，数据保留 `round=1`。

2026-09-25 完整回归 **122 项全部通过，16.166 秒**：配置 24 项、协议质量 18 项、控制器 28 项、记录器 24 项、HTTP 11 项、启动实例复用 17 项。之后加强 HTTP 负例，将已退役的视频真实放入临时目录再验证请求仍被拒绝，11 项 HTTP 测试再次全部通过（0.612 秒）。`node --check web/app.js`、`setup.ps1 -CheckOnly`（8/8 视频）及 `git diff --check` 均通过。

新增和更新的验证涵盖两种随机开头、三天素材分配、编号首尾空格与大小写规范化、重复检查与新页面顺序一致、无轮次字段正常开始、旧协议记录隔离、过期或他人计划不能开始、四份独立量表及播放计划指纹保存校验。启动复用修复继续纳入回归。

当前素材仅需 `attention_video/01.mp4`–`06.mp4`、`relax_video/01.mp4`–`02.mp4`。配置测试确认未使用的八个旧素材缺失时仍可运行，缺失当前必需素材则报告错误。旧素材从当前 Git 跟踪中移除，本机原文件保留；历史采集结果不迁移、不覆盖。

另外从待提交 Git 索引导出独立目录 `work/single-round-release-check/`，确认它只有上述八个原视频，再使用项目 Python 运行该副本的 `scripts/check_environment.py`，应用导入、配置和视频检查通过（8/8）。这项检查验证移除旧素材后的发布内容可以完成自检，未重新安装已有依赖。

实际浏览器在隔离目录 `work/single-round-browser-fixture/` 完成两种起始顺序。该目录的 14 个 app/web/config 文件 SHA256 与本次源码一致，仅使用八段 4 秒测试视频和模拟 EEG；每轮真实等待一次 30 秒基线，未倍速、未跳转视频、未使用假时钟。原始实验视频未修改。

| 测试编号（Day 1） | 实际顺序 | 四份评分（确信度均为4） | EEG行数 | 标签行数 |
|---|---|---|---:|---:|
| UI_V3_ATTENTION_4 | A01 → R01 → A02 → R02 | 2、5、1、4 | 44,140 | 251 |
| UI_V3_RELAX_0 | R01 → A01 → R02 → A02 | 5、2、4、1 | 40,780 | 238 |

浏览器登记仅包含被试和 Day，重复检查保持原顺序。四份量表初始均为空，只有第四份提交后结束。两次完成页均显示四个 trial、四份量表和三文件保存成功。独立运行 `work/verify_single_round_browser.py`，复读 CSV/JSON 核对独立 trial/视频/评分、样本行号、随机化信息、计划指纹和恰好 30 秒基线；结果保存至 fixture 的 `work/browser-result-verification.json`。

原项目已重启并刷新浏览器，八个原视频均通过时长与首帧预检，另实际核对 Day 3 的 R01 → A05 → R02 → A06 计划及单轮登记布局。短视频全流程测试不等同于逐秒完整播放所有原视频。所有测试评分均为软件测试输入，真实耳机、左右耳归属、实际声音输出和物理时间同步仍需实机验收。

## v1.1.0 历史测试记录：每天两轮、四 trial

以下为上一版协议及后续启动修复的历史验证，不代表新版素材和随机起始规则的测试结果。

测试日期：2026-09-25。测试环境：Windows、项目 `.venv` 中的 Python 3.12.5、Tornado 6.5.2、Bleak 1.1.1。

当前协议为 `earwise-3day-2round-4trial-v2`：**3 天 × 每天 2 轮 × 每轮 4 个 trial**，每个 trial 后各一份量表。第 1 轮为 attention → relax → attention → relax，第 2 轮为 relax → attention → relax → attention；每轮开头仅一次 30 秒基线，中间不再插入基线。

### 自动化与环境检查

四 trial 协议修改时的完整回归 **92 项测试全部通过**，`unittest` 报告运行时间 **14.140 秒**，进程退出码为 0。

| 测试文件 | 数量 |
|---|---:|
| `tests/test_configuration.py` | 18 |
| `tests/test_protocol_quality.py` | 18 |
| `tests/test_controller.py` | 25 |
| `tests/test_recorder.py` | 20 |
| `tests/test_http.py` | 11 |
| **合计** | **92** |

以下检查均通过：

- `node --check web/app.js`：前端 JavaScript 语法检查。
- `setup.ps1 -CheckOnly`：项目现有环境和资源自检。
- `git diff --check`：本次差异空白格式检查。

测试覆盖新版六轮素材映射、四 trial 顺序及量表关联、第四次评分后收尾、三文件完整性校验和旧协议记录隔离。控制器自动化测试通过可推进时钟验证基线与播放边界，不能单独证明浏览器真实等待或媒体完整播放；模拟 BLE 也不能替代真实耳机验收。

### 重复启动修复验证

针对“后台服务已在运行，再次双击启动脚本只报错且不打开网页”的问题，新增 17 项测试后完整回归 **109 项全部通过**，运行时间 **20.261 秒**。新增覆盖服务身份和实际端口核验、重复启动不创建控制器或重跑数据恢复、模拟/真实模式冲突、旧记录或失联服务、浏览器打开失败的地址提示，以及本地实例接口的访问限制。

在本机使用实际 `start-simulation.ps1` 启动模拟服务，再次运行不带 `-NoBrowser` 的脚本，成功进入重新打开浏览器分支并以退出码 0 结束；前后实例 ID 相同，没有创建采集会话。随后执行 `start-simulation.bat -NoBrowser`，也以退出码 0 复用同一服务。已在浏览器打开本地首页并确认“模拟模式”和新版登记界面。以上启动测试未连接真实耳机，未开始实验采集。

### 本次浏览器验证

已在隔离目录 `work/four-trial-browser-fixture/` 中，通过实际浏览器分别完成 Day 1 / Round 1 与 Round 2。使用模拟 EEG、16 个每段 4 秒的 H.264/AAC 测试视频；原项目的 16 个实验视频未替换。每轮真实等待一次 30 秒基线，视频按原速完整播放，未跳转进度或使用假时钟。

| 轮次 | 浏览器播放顺序 | 四份测试评分（确信度均为 4） | EEG 行数 | 标签行数 |
|---|---|---|---:|---:|
| Round 1 | attention_01 → relax_01 → attention_02 → relax_02 | 2、5、1、4 | 68,960 | 347 |
| Round 2 | relax_03 → attention_03 → relax_04 → attention_04 | 5、2、4、1 | 39,470 | 230 |

- 每份量表初始无已选项；第二份提交后继续第三个 trial，只有第四份显示“提交并完成本轮”。两轮结束页面均显示四个 trial、四份量表及三文件保存成功。
- 独立读取生成的 CSV/JSON，确认每轮恰好三个文件、状态 `completed`、协议标识正确、基线起止恰好相差 30 秒，四个独立 trial 的视频、顺序、评分和确信度逐项匹配；另一条件的评分字段为空。
- 核验脚本为 `work/verify_four_trial_browser.py`，结果记录为 `work/four-trial-browser-fixture/browser-result-verification.json`；测试目录在 `work/` 下，不作为正式数据或发布内容。
- 原项目浏览器已重新检查全部 16 个原视频的时长与首帧，并核对第一轮和第二轮的四视频计划。当前新版未逐秒完整播放全部原视频；下方 v1.0.0 两 trial 的原视频验收属于历史证据。

以上评分为软件测试输入，不是受试者研究结果。浏览器音轨保持启用，但未进行人工听觉确认；真实耳机连接、通道左右归属和硬件采集仍待设备到位后验证。

## v1.0.0 历史测试记录（每轮两 trial）

以下完整保留旧版报告，描述旧版每天 4 轮、每轮 2 个 trial 的协议及当时测试。文中的“最终版本”“全部测试”“完整轮次”均指 v1.0.0，不能据此宣称 v1.1.0 的四 trial 浏览器或硬件验收已完成。

测试日期：2026-09-25。测试环境：Windows、项目 `.venv` 中的 Python 3.12.5、Tornado 6.5.2、Bleak 1.1.1。

### 自动化测试结果

#### EarWise 一键配置与发布检查

2026-09-25 使用 Windows PowerShell 5.1 验证 `setup.bat` / `setup.ps1`：

- 现有虚拟环境的版本检查、`pip check`、应用/WinRT 模块导入、配置和全部 16 个原视频检查通过；`.bat` 包装入口运行成功。
- 在包含中文和空格的隔离副本中，从其他工作目录启动，创建全新 `.venv` 并安装 11 项固定依赖成功；重复执行和 `-CheckOnly` 均通过。
- 缺失视频时返回非零退出码，并指出缺失文件；损坏的已有 `.venv` 返回失败，哨兵文件保持不变，没有被自动删除或覆盖。
- 更新后的模拟启动脚本正常提供本地 HTTP 服务；首页返回 200，标题为“EarWise采集系统”，状态接口可访问；验证后已关闭服务。
- 官方 Python 3.12.10 x64 安装包已实际下载，Authenticode 状态为 `Valid`，发布者为 Python Software Foundation。当前电脑已有 Python，未实际执行“无 Python 新电脑”的安装器分支。

这些检查不扫描或连接真实耳机，也不代表已完成硬件验收。原始参考脚本和原始需求文档保持原内容，不为消除历史空白格式提示而改写。

#### 应用回归测试

首次软件验收 **70 项测试，全部通过**；`unittest` 报告运行时间为 8.794 秒，进程退出码为 0。EarWise 更名及安装脚本发布前再次运行同一套 70 项测试，全部通过，运行时间 12.075 秒。

```powershell
Set-Location -LiteralPath '.\EarWise--'
& .\.venv\Scripts\python.exe -m unittest discover -s tests -p 'test*.py' -v
```

| 测试文件 | 数量 | 验证内容 |
|---|---:|---|
| `tests/test_configuration.py` | 10 | 12 轮视频对应与顺序、输入和配置校验、缺失/错误素材、MP4 元数据、摘要与缓存失效 |
| `tests/test_protocol_quality.py` | 18 | 两通道有符号 24 位解码、拆包/粘包、异常字节、饱和率、序号回绕/缺口/重复/复位、模拟数据、BLE 连接不自动发命令 |
| `tests/test_controller.py` | 21 | 奇偶轮全流程、30 秒基线边界、媒体与量表状态、时钟映射、重复请求、只读页面、刷新/断连/超时、重采、写入失败、素材变更、设备操作互斥、真实模式硬件验证门槛 |
| `tests/test_recorder.py` | 10 | 三文件、真实临时 CSV 写入和读取、评分持久化、数据完整性、队列满/积压、写盘异常、旧文件保护、遗留会话恢复 |
| `tests/test_http.py` | 11 | 临时本地 HTTP/WS 服务、JSON 请求校验、Origin/Host 拒绝、媒体 Range 206、限定素材路径、页面状态、项目进程锁、编辑页面后的 ETag 失效 |

控制器测试使用可推进的服务端单调时钟与 6 秒媒体元数据夹具，逐步推进 **30 秒逻辑基线**并完成两个 trial，不会为每个测试真实等待 30 秒，也没有在该测试中解码播放原视频。该方式验证计时边界和状态逻辑，不能单独作为真实浏览器全程播放的证据。

控制器和记录器测试确实启动独立写入线程，写入临时目录内的 CSV/JSON，再读取文件验证，不是仅检查内存状态。每个测试结束清理自己的临时目录，不写入正式 `data/`，不接触正在运行的浏览器采集会话。配置测试的临时 MP4 只含用于元数据解析的结构，不是替代正式素材的示例视频。

HTTP 测试使用临时端口、临时页面和字节文件。已验证浏览器请求视频所需的 Range 传输行为，但这本身不证明媒体解码兼容性。BLE 测试用模拟客户端验证连接、通知订阅和命令行为，没有连接真实耳机。

### 与需求 A01–A16 对照

| 编号 | 自动化证据 | 证据范围与后续验收 |
|---|---|---|
| A01 | 项目 `.venv` 运行全部测试；本地 HTTP 页面和状态接口正常；第二服务进程锁被拒绝 | 双击启动与实际浏览器视觉结果属于独立运行验收 |
| A02 | 空值、非法编号、错误类型、路径穿越及 Windows 保留名称被拒绝；`001` 保留前导零 | 已覆盖后端输入与路径边界 |
| A03 | 全部 12 个组合正确；attention 01–12 各一次、relax 按 round 重复；缺素材指出文件；错误配置不回退；预检后大小/修改时间/创建时间/时长改变均禁止开始 | 原始素材浏览器解码与实际音轨须结合浏览器验收 |
| A04 | 全部映射测试及奇数/偶数控制器全流程均验证顺序 | 已覆盖后端顺序规则 |
| A05 | 29.75 秒仍处基线，30 秒进入 transition；边界相差恰好 30,000,000,000 ns；样本不足 7500 行仍按时结束；预览数据排除 | 使用假时钟；真实等待另验 |
| A06 | 每轮两个完整逻辑 trial、各一份量表；提前 ended 和倍速事件拒绝；缓冲恢复不重复 trial | 前端完整播放另验 |
| A07 | 1–5 整数校验、拒绝 bool/浮点/缺值；量表重复提交幂等；已提交评分不可改变；无关评分字段为空 | 记录器同时复查持久化评分 |
| A08 | 24 位最小/最大、正/负/零；每种拆分位置；粘包；帧尾高四位；只有两路真实字段 | 实际硬件数据与物理单位仍待验证 |
| A09 | 254→255→0→1 回绕、10→13 缺两帧、重复不稀释分母、复位/乱序/长间隔标未知；整圈歧义不算精确损失 | 默认可信连续性参数仍需真实 BLE 批量到达验证 |
| A10 | 两通道独立已知阈值比例、边界样本、无数据为空、滑动窗口过期、配置与快照算法一致 | 98% 阈值为工程初值，真实削顶定义未确认 |
| A11 | 无数据、接收、过期状态；刚连接无有效帧显示 waiting 且无法开始；断连清除半帧缓存并停止会话；过期质量值为空；未知映射/命令/采样率/削顶定义阻止真实模式，设备类型不会静默切换成模拟 | 蓝牙测试使用模拟状态和 mock 客户端，未连接真实耳机 |
| A12 | 前端映射事件时刻与服务端接收时刻独立保存；误差字段；过渡、播放、缓冲、问卷事件可区分 | 软件级时间标记；未验证屏幕物理呈现时刻 |
| A13 | 奇偶轮完成后恰好三文件，身份和行号一致，两 trial 与评分各一次；错误关联或评分导致 save_failed | 临时目录中真实写入/关闭/复读；正式硬件轮次待验 |
| A14 | BLE 断开、无数据、首个正式帧迟到、看门狗间隔中迟到都中断；重采新目录且关联前次；旧文件字节不变 | 无跨中断自动续采 |
| A15 | 控制页关闭/刷新、心跳缺口、写盘 OSError、满队列、积压；只恢复 recording 遗留状态，保留终态原因 | 文件写入错误为故障注入；未做真实磁盘写满、断电或强杀持久性实验 |
| A16 | 相同开始请求返回既有 session；其他页面不可控制/维持心跳；重复媒体和评分不会跨阶段；项目进程锁互斥；连接/发送命令与开始采集互斥且预览阶段状态 tick 不被扫描锁阻塞 | 后端控制与请求重试覆盖；实际多页视觉反馈另验 |

### 回归检查发现并修复的问题

- 过早 `ended` 事件被拒绝后不应修改有效播放累计时间，已验证随后正常结束仍能进入量表。
- 开始成功前需将正式单调时钟起点和基线阶段持久化；已验证初始 `config.json` 即含这些字段。
- 配置快照需使用实际运行的丢包算法版本，已与质量事件逐项核对。
- 晚到的数据或心跳不能清除已经发生的超时；已覆盖首个正式样本和看门狗尚未执行的间隔。
- 保存校验需验证每个 trial 的唯一开始/结束/评分及评分有效性，不能只核对事件总数。
- HTTP 媒体路径限定到规定的 16 个文件，已验证额外 `99.mp4` 即使存在也不会被提供。
- 本地页面改动后需使旧 ETag 失效；已验证未修改时为 304，修改后携带旧 `If-None-Match` 返回 200 和更新内容。

### 真实耳机验收未完成

本报告不宣称真实硬件通过。正式采集前仍需核对左右映射、原始模式与启动命令、250 Hz 名义采样率、ADC 削顶范围、特殊状态值及 BLE 批量到达行为；换算未经确认时保留原始 counts，μV 为空。

实验负责人需完成真实耳机的至少一个奇数 round 和一个偶数 round，确认声音、完整视频播放、固定质量状态区、断连处理及最终三文件。软件测试不能替代人体 EEG 的真实性、耳机佩戴或物理同步验证。

### 实际浏览器运行验收

已在本机 Codex 浏览器实际操作，不修改播放速度、不跳转媒体位置，使用模拟设备完成 Day 1 / Round 1：

- 全部 16 个原素材通过浏览器时长和首帧解码预检，包含 AV1 与 H.264。
- 真实等待一次 30 秒基线，显示用户确认的“静息30s”。文件中的基线起止单调时间相差恰好 30,000,000,000 ns。
- attention_video/01.mp4（272.687 秒）和 relax_video/01.mp4（300.002 秒）均按原速实际完整播放；服务端累计播放时间分别为 272.890 秒、300.282 秒。
- 两份量表初始均无选项；第一份提交后才进入第二个视频。测试专心评分/确信度为 3/4，放松评分/确信度为 4/5。这些是软件测试输入，不是受试者研究结果。
- 结果页面显示“本轮采集完成，数据已保存”；独立重新读取三文件，核对连续行号、会话身份、两份量表归属以及空 μV 字段。
- 最终原始数据 **172,640 行**，标签 **698 行**，目录恰好 `eeg_raw.csv`、`labels.csv`、`config.json`；配置状态 `completed`、模式 `simulation`。模拟流统计无重复、推定缺帧或未知缺口。

保留的完整模拟示例位于：

```text
simulation_data/sub_SIM_UI_001/day_01/round_01/20260925T120624_d635928b62cc/
```

在最终版本中另用 `SIM_UI_REFRESH` 验证 Day 1 / Round 2 的“放松→专心”计划，进入基线后刷新控制页面，后端按“控制页面刷新或关闭”中断并保留独立三文件。另一个实际打开的页面仅显示只读状态，不能维持或接管原会话。该测试是中断测试，未播放完整偶数轮；完整偶数轮状态流程由自动化测试覆盖。

最终桌面和窄屏截图均已人工查看，采集时连接状态、两通道饱和率、共享丢包率、左右待确认提示均在固定状态区可见。为避免在完整模拟轮次中刷新导致中断，最后的页面紧凑布局与并发边界修复通过独立短程浏览器检查及最终自动化回归验证。

浏览器检查保留音轨且媒体未静音，但没有人工听觉确认实际电脑音量；声音输出须由真实实验操作人确认。除了上述两个完整播放素材，其余视频只验证了元数据与首帧，不能据此声称所有素材均已逐秒播放验收。以上证据全部是软件/模拟验收，不能替代真实耳机奇数轮和偶数轮验收。
